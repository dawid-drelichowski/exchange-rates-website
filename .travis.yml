language: php
services: mysql
php:
  - 5.6
  - 5.5
  - 5.4
before_install:
  - composer self-update
before_script:
  - mysql < db/dump.sql
  - composer install
  - npm install
  - cp config/default.json.dist config/default.json
script:
  - karma start